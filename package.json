{
  "name": "tank-battle-sui",
  "version": "1.0.0",
  "description": "AI-Driven Web3 Tank Battle Game on Sui",
  "scripts": {
    "build": "cd move_contracts && sui move build",
    "test": "ts-node scripts/deployment/test-functions.ts",
    "deploy": "node scripts/deployment/final-deploy.js",
    "check": "node scripts/deployment/check-status.js",
    "test-gun": "node scripts/deployment/test-gun.js",
    "status": "sui client objects",
    "balance": "sui client balance"
  },
  "dependencies": {
    "@mysten/sui.js": "^0.54.0",
    "dotenv": "^16.6.1",
    "firebase": "^12.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
