[package]
name = "tank_battle"
version = "1.0.0"
edition = "2024.beta"

[dependencies]
Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "framework/testnet" }

[addresses]
tank_battle = "0x0"
sui = "0x2"

# Core Modules:
# - tank_battle::game (Tank & Gun NFTs)
# - tank_battle::market (NFT Marketplace)
# - tank_battle::battle (Battle Results)
# Uses native SUI token for payments